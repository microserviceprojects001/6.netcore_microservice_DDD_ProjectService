# 我能看到的是这个值对象，要不就适合 聚合根 放在同一个表中，存放在不同的 字段中，要不就是也跟聚合根同表中 在一个 json 字段中，

# 代码实例

```
src/
├── Domain/
│   ├── Orders/
│   │   ├── Order.cs              # 聚合根
│   │   ├── OrderId.cs           # 订单ID值对象
│   │   ├── OrderItem.cs         # 内部实体
│   │   ├── Address.cs           # 地址值对象
│   │   ├── OrderStatus.cs       # 枚举
│   │   └── Events/              # 领域事件
│   │       ├── OrderCreatedEvent.cs
│   │       └── ShippingAddressChangedEvent.cs
│   ├── Customers/
│   │   └── CustomerId.cs        # 客户ID值对象
│   └── Products/
│       └── ProductId.cs         # 产品ID值对象
├── Infrastructure/
│   ├── Repositories/
│   │   └── OrderRepository.cs   # 仓储实现
│   └── Data/
│       └── Schema.sql          # 数据库脚本
└── Application/
    └── Services/
        └── OrderService.cs
```
