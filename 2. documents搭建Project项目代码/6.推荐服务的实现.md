# 初始化推荐服务

dotnet new webapi -n Recommend.API --use-controllers --force

dotnet add package DotNetCore.CAP --version 8.4.0
dotnet add package DotNetCore.CAP.Dashboard --version 8.4.0
dotnet add package DotNetCore.CAP.MySql --version 8.4.0
dotnet add package DotNetCore.CAP.RabbitMQ --version 8.4.0
dotnet add package Pomelo.EntityFrameworkCore.MySql --version 8.0.0
dotnet add package Microsoft.EntityFrameworkCore.Design --version 8.0.0

dotnet add package consul --version 1.7.14.9
dotnet add Recommend.API.csproj reference ../BuildingBlocks/Resilience/Resilience.csproj
dotnet add package polly --version 8.6.3

# 初始化数据库

dotnet ef migrations add initRecommendDb
dotnet ef database update

# 像这种集成事件的定义，我觉得最好是抽出到一个公共的类库中哈，以为发送端修改了什么，有可能接收端不知道呀，有任何改动，都不透明

您说得非常对！这是微服务架构中一个很重要的问题。将集成事件定义放在公共类库中是最佳实践。

推荐方案：创建共享类库

1. 创建共享类库项目
2. 在各项目中引用共享类库
