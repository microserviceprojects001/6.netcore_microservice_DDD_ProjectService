# 初始化数据库遇到的问题

dotnet ef migrations add initProjectDb

在 Project.API 中代码如下：

```
var connectionString = builder.Configuration.GetConnectionString("MySQLProject");
Console.WriteLine($"DB Server: {connectionString}");

builder.Services.AddDbContext<ProjectContext>(options =>
    options.UseMySql(connectionString, ServerVersion.AutoDetect(connectionString))
);
```

执行数据库初始化时候报错

```
No DbContext was found in assembly 'Project.API'. Ensure that you're using the correct assembly and that the typt the type is neither abstract nor generic.
```

# 原因

这个错误是因为 dotnet ef migrations 命令默认在当前程序集（Project.API）中查找 DbContext，但您的
ProjectContext 在 Project.Infrastructure 程序集中。

# 寻找方案(不 work)

```
dotnet ef migrations add initProjectDb --project Project.Infrastructure --startup-project Project.API
```

# 遇到问题

嗯，我现在是在 Project.API 中执行了

dotnet ef migrations add initProjectDb --startup-project . --project ../Project.Infrastructure --context ProjectContext

这个命令，现在报错 Unable to create a 'DbContext' of type 'ProjectContext'. The exception 'Unable to resolve service for type
'Microsoft.EntityFrameworkCore.DbContextOptions`1[Project.Infrastructure.ProjectContext]' while attempting
to activate 'Project.Infrastructure.ProjectContext'.' was thrown while attempting to create an instance. For the different patterns supported at design time, see https://go.microsoft.com/fwlink/?linkid=851728

# 经过添加 ProjectContextDesignTimeFactory 后执行以下命令，能成功的更新本地数据库了

```
D:\Code\1.microservice\2.netcore_microservice_userService\Project\Project.API

Server=localhost;port=3307;Database=beta_project;User=abel;Password=acsdev312!@;

dotnet ef migrations add initProjectDb --startup-project . --project ../Project.Infrastructure
dotnet ef database update --startup-project . --project ../Project.Infrastructure

```

此时如果 我把数据库连接直接换成 阿里云服务器的数据库连接，然后执行以上的 update 命令，是可以更新数据库的
Server=8.140.59.193;port=3307;Database=beta_project;User=abel;Password=acsdev312!@;

# 解释一下 EF Core 数据库生成的工作原理和必要条件

1. 核心必要条件
   数据库生成真正需要的是：

✅ DbContext 类

✅ 数据库连接字符串

✅ 数据库提供程序配置

✅ 迁移文件

不需要的是：

❌ 应用程序中的 DI 注册 (AddInfrastructure)

❌ 应用程序启动

2. 为什么没有 AddInfrastructure 也能生成数据库？

```
因为 EF Core 迁移工具 和 应用程序运行时 是两个独立的上下文：

场景	             配置来源	                                   DbContext 创建方式
EF Core迁移工具	    设计时工厂 (ProjectContextDesignTimeFactory)	直接实例化
应用程序运行时	     DI 容器 (AddInfrastructure)                 	依赖注入
```

3. 各代码的作用分析

ProjectContext.cs - 必需
ProjectContextDesignTimeFactory.cs - 迁移时必需

# 遗留问题

客户的线上数据库要如何 更新呢
我现在本地对数据库的初始化 这么去操作完全没有问题，那客户那里的线上数据库要怎么办呢，如果客户那里是 完全属于 新的环境，那还可以，从无到有，好像还可以，但是客户那里也不能让执行
dotnet ef migrations add initProjectDb --startup-project . --project ../Project.Infrastructure
dotnet ef database update --startup-project . --project ../Project.Infrastructure 这些命令呀

# 方案

生成 SQL 脚本（最推荐）
在开发环境生成迁移脚本

```
# 生成完整的迁移脚本
dotnet ef migrations script --startup-project . --project ../Project.Infrastructure --output migration.sql
```

需要实现执行
dotnet ef migrations add initProjectDb --startup-project . --project ../Project.Infrastructure

# 完整的工作流程

```
# 1. 创建迁移（生成迁移文件）
dotnet ef migrations add InitProjectDb --startup-project . --project ../Project.Infrastructure

# 2. 生成 SQL 脚本
dotnet ef migrations script --startup-project . --project ../Project.Infrastructure --output migration.sql

# 3. 应用迁移（可选，用于测试）
dotnet ef database update --startup-project . --project ../Project.Infrastructure
```

# 生产环境部署策略

策略 1：一次性完整部署（新项目）

```
# 开发环境
dotnet ef migrations add InitProjectDb
dotnet ef migrations script --idempotent --output full_deployment.sql

# 交付 full_deployment.sql 给客户
```

策略 2：增量部署（已有项目更新）

```
# 开发环境：创建新迁移
dotnet ef migrations add AddNewFeature
dotnet ef migrations script FromPreviousMigration --idempotent --output incremental_update.sql

# 交付 incremental_update.sql 给客户
```

# 策略 1：一次性完整部署（新项目） 我觉得还好，但是 策略 2：增量部署（已有项目更新） ，那就是每一次数据库更新的 migration 都得保存了呀，因为我们可能对数据更改很频繁呀，

策略 1：定期合并迁移（推荐）在开发阶段频繁创建迁移，发布前合并

```
# 开发期间：频繁创建迁移
dotnet ef migrations Add AddUserEmailIndex
dotnet ef migrations Add FixProjectDescriptionLength
dotnet ef migrations Add AddAuditColumns

# 发布前：删除所有迁移，创建一个合并迁移
dotnet ef migrations remove --force
dotnet ef migrations remove --force
dotnet ef migrations remove --force
dotnet ef migrations add Release_v1.2.0
```

# 我们就走策略 1：定期合并迁移（推荐） ，这个方案哈，我想知道知道一下呀， 发布前：删除所有迁移，创建一个合并迁移，我都给删除了，然后 dotnet ef migrations add Release_v1.2.0 这是什么意思呢，我没理解，此时我本地数据库已经是 最新的了呀，我再执行 dotnet ef migrations add Release_v1.2.0 会怎么样生成迁移文件呢

步骤 1：删除迁移文件前的状态
Migrations/
├── 20241201000000_InitialCreate.cs
├── 20241202000000_AddUserEmail.cs  
├── 20241203000000_AddProjectStatus.cs
├── 20241204000000_FixDescriptionLength.cs
└── ProjectContextModelSnapshot.cs ← 这个文件很重要！
步骤 2：删除迁移文件（保留快照）

```
# 删除所有迁移文件，但快照文件会自动保留
find Project.Infrastructure/Migrations -name "*.cs" ! -name "*Snapshot.cs" -delete
```

步骤 3：执行 dotnet ef migrations add Release_v1.2.0
这时 EF Core 会：

读取快照文件：了解当前数据库应该是什么结构

与空状态比较：因为删除了所有迁移，EF Core 认为数据库是空的

生成完整迁移：创建一个包含所有表结构创建的迁移

# 我先理解一下哈，我把迁移文件都删除了，保留快照文件后，此时再去执行 dotnet ef migrations add Release_v1.2.0 生成迁移文件，意思是说就是把之前的迁移都合并到一起了哈

完全正确！你的理解 100%准确！ 🎉

# 好，那如此 我觉得不会有什么问题了，只需要一个统一的人，定期的去做这个事情就可以了
